[theme]
base = "light"
primaryColor = "#c8553d"
font = "sans serif"

[server]
headless = true

[client]
toolbarMode = "minimal"

[logger]
# Also hides Streamlit's developer "install skills" popup, which visitors should not see.
hideWelcomeMessage = true
